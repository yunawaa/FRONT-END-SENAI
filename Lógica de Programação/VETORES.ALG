algoritmo "aula exemplo de vetor"

var
           teste, contador:  inteiro
           numeros : vetor [1..3] de inteiro
inicio
           para contador <- 1  ate 3 faca
           leia (numeros[contador])
           fimPara
           para contador <- 1 ate 3 faca
           escreval (numeros[contador])
           escreval (numeros [1] + numeros [3])
           fimPara
fimalgoritmo